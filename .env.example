# Frontend → Laravel API
# Local:
# API_URL=http://localhost:8000
# NEXT_PUBLIC_API_URL=http://localhost:8000
#
# Production (Vercel): must be the PUBLIC API host — never a cluster/private DNS.
# Private hosts cause: DNS_HOSTNAME_RESOLVED_PRIVATE and registration 404s.
API_URL=https://api.noel54.com
NEXT_PUBLIC_API_URL=https://api.noel54.com
